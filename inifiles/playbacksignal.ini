[general]
debug=2

[redis]
hostname=localhost
port=6379

[fieldtrip]
hostname=localhost
port=1972

[playback]
;file=../record/record_2017.12.16_12.09.55.edf
file=/home/hyruuk/GitHub/eegsynth/datafiles/testBipolar20170827-0.edf
blocksize=0.1       ; in seconds

; the speed relative to the original sampling frequency can be changed
speed=1

; the value for play should be non-zero to play, it can be assigned to a toggle button
;play=1
play=1

; the value for play should be non-zero to pause, it can be assigned to a toggle button
;pause=0
pause=launchcontrol.note042

; the value for rewind should be zero as long as you don't want to rewind, it can be assigned to a push button
;rewind=0
rewind=0

; the physical min and max only apply to a wav file and determine how the values from the file will be scaled
physical_min=-1
physical_max=1
