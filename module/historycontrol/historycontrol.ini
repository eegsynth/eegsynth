[general]
debug=2

[redis]
hostname=localhost
port=6379

[smoothing]
window=5            ; window length for smoothing (s)
stepsize=0.1        ; update time (s)

[input]
; the freeze button can be assigned to the launchcontrol to freeze the updating
;freeze=launchcontrol.note073
freeze=0

; control values to plot, separated by comma
; channels=launchcontrol.control077,launchcontrol.control078
channels=theta,alpha,beta
