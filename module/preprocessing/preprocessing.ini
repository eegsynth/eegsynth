[general]
delay=0.05
debug=1

[redis]
hostname=localhost
port=6379

[input_fieldtrip]
hostname=localhost
port=1972
timeout=30

[output_fieldtrip]
hostname=localhost
port=1973

[processing]
window=0.25         ; in seconds
smoothing=0.20      ; see https://en.wikipedia.org/wiki/Exponential_smoothing
reference=median    ; <none|average|median>
lowpassfilter=45    ; in Hz
highpassfilter=0.5  ; in Hz
filterorder=511     ; prefer odd number
downsample=4        ; downsampling factor (integer)
