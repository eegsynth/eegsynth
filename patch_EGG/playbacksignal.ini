[general]
debug=2

[redis]
hostname=localhost
port=6379

[fieldtrip]
hostname=localhost
port=1972

[playback]
;file=../record/record_2017.12.16_12.09.55.edf
file=../../data/IR.edf
speed=1             ; relative to the original sampling frequency
blocksize=0.1       ; in seconds

; the value for play should be non-zero to play, it can be assigned to a toggle button
; the rewind option should be zero as long as you don't want to rewind, it can be assigned to a push button
play=1
rewind=0

;play=launchcontrol.note043
;rewind=launchcontrol.note075

; the physical min and max only apply to a wav file and determine how the values from the file will be scaled
physical_min=-1
physical_max=1
